steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -f
      - deploy/Dockerfile
      - -t
      - $_IMAGE
      - .

images:
  - $_IMAGE

options:
  machineType: E2_HIGHCPU_8   # faster build; still within free quota for first 120 min/day
